# Recreate `summary_table.xlsx` to Match References & Notes

## Background

The current `outputs/summary_table.xlsx` is generated by `write_summary_workbook()` in [`excel_reports.py`](file:///Users/bunnypro/Projects/LOGage2026/src/logage2026/excel_reports.py). The reference Excel files in `references/` represent the **authoritative answer template** for each question, and the `.tex` notes define the narrative, numbers, and structures that must appear in the workbook.

The goal is to re-align all summary_table sheets to precisely match the structure, content, and semantics from the reference files and notes — updating both the generator code in `excel_reports.py` and the sheet configuration in `write_summary_workbook()`.

---

## Gap Analysis: Current vs. Reference

### Part 1 — Q1.1 ABC-XYZ

| Reference sheet | Current sheet | Status |
|---|---|---|
| `Q1.1 ABC-XYZ` (42 rows, 16 cols) | `Q1.1 Dashboard` (44 rows, 9 cols) | ⚠️ Needs restructuring |
| `Full SKU Ranking` (473 rows, 25 cols) | `Q1.1 Full SKU Ranking` (472 rows, 19 cols) | ⚠️ Missing columns |

**Reference `Q1.1 ABC-XYZ` sections (what must be in the dashboard):**
- **Section A** — ABC by Quantity: Class A/B/C with SKU count, %, Total Qty, cum%, CBM, threshold
- **Section B** — ABC by Frequency: Class A/B/C with SKU count, %, Total Freq, cum%, Freq/SKU, meaning
- **Section C** — XYZ Volatility: Class X/Y/Z with CV range, SKU count, pattern, safety stock policy, replenishment
- **Section D** — ABC-XYZ (Qty × Variability) matrix: 3×3 cross-tab
- **Section E** — ABC Qty × Freq (Fast-Moving) matrix: 3×3 cross-tab  
- **Section F** — Fast-Moving Group (A-A: AA class): SKU count, volume pcs, order freq, avg monthly demand, top 10 SKUs

**Reference `Full SKU Ranking` has 25 columns** — current has 19. The 6 missing columns in the reference are the monthly demand columns (Jun–Dec 2025). We need to add them.

> [!NOTE]
> The reference `Full SKU Ranking` rows count (473) differs from current (472) by 1 — likely a header row. The 470 SKUs + 2 headers + 1 total row = 473. Investigate during implementation.

---

### Part 1 — Q1.2 Distribution Heatmap

| Reference sheet | Current sheets | Status |
|---|---|---|
| `Q1.2 Heatmap` (36 rows, 14 cols) | `Q1.2 Warehouse by Region` (112 rows) | ⚠️ Needs consolidation |
| | `Q1.2 Top Provinces` (18 rows) | |
| | `Q1.2 Warehouse Imbalance` (14 rows) | |

**Reference `Q1.2 Heatmap` sections:**
- **Section A** — Warehouse balance (My Phuoc vs Vinh Loc): Qty, %, CBM, order count, %
- **Section B** — Demand by region: Vùng, Qty, % Total, Orders, Qty/Order
- **Section C** — Top 15 provinces: Rank, Tỉnh/Thành, Qty, % Total, Cum%, Orders, Qty/Order, Dist MP (km), Dist VL (km)

**Decision**: Keep the current 3-sheet structure but add a new **`Q1.2 Heatmap Summary`** sheet that mirrors the consolidated reference structure — with all three sections A/B/C. This is the "answer sheet" format expected by the judges.

---

### Part 1 — Q1.3 Order Profile

| Reference sheet | Current sheets | Status |
|---|---|---|
| `Q1.3 Order Profile` (20 rows, 14 cols) | `Q1.3 Segment Profile` (4 rows) | ⚠️ Needs consolidation |
| | `Q1.3 Packaging` (8 rows) | |
| | `Q1.3 Geo Spread` (4 rows) | |

**Reference `Q1.3 Order Profile` sections:**
- **Section A** — Comparison table: Tiêu chí, Formula, Modern Trade, Traditional, MT vs TT, Operational meaning (14 cols)
- **Section B** — Pack unit mix (for charts): Segment, Pallet, Carton, Loose

**Decision**: Add a new **`Q1.3 Order Profile Summary`** sheet mirroring the reference consolidated format, keeping the 3 existing detailed sheets.

---

### Part 2 — Q2.2 Safety Stock & Pooling

| Reference sheets | Current sheets | Status |
|---|---|---|
| `Lead Time theo Vùng` (20 rows, 10 cols) | *(missing)* | ❌ Missing entirely |
| `Q2.2 Safety Stock` (38 rows, 12 cols) | `Q2.2 Safety Stock` (30 rows, 15 cols) | ⚠️ Structural differences |
| `Q2.2 Pooling` (28 rows, 12 cols) | `Q2.2 Inventory Pooling` (5 rows) | ⚠️ Much less content |

**Reference `Lead Time theo Vùng`** contains:
- Methodology header (mile-weighted average formula explanation)
- Region table: Vùng, Route type, LT (days), Orders, Weight, Quantity, LT×Orders, Source
- Result summary: LT_avg = 1.94 days

**Reference `Q2.2 Safety Stock`** has 12 columns:
- SKU, Product, ABC-XYZ, μ_monthly, σ_monthly, μ_daily, σ_daily, Z, √LT, Safety Stock, ROP, + formula header rows

**Reference `Q2.2 Pooling`** has detailed sections including:
- Channel definitions (MT, Ecommerce, TT)
- Allocation priority under shortage (4-priority ladder)
- Operational mechanisms (ROP floor, alert, end-of-day rebalance)
- Quantification: virtual pooling saves ~62% safety stock (detailed formula rows)

---

### Part 2 — Q2.1 Network Model

| Reference | Current sheets | Status |
|---|---|---|
| *(no Part 2 reference for Q2.1)* | `Q2.1 HCM Districts` | ✅ OK |
| | `Q2.1 Dark Store SLA` | ✅ OK |

---

### Part 3 — Q3.1 Slotting

| Reference sheets | Current sheets | Status |
|---|---|---|
| `Q3.1 Slotting Design` (20 rows, 7 cols) | `Q3.1 Travel-Time Summary` (31 rows) | ⚠️ Structural difference |
| `Q3.1 Travel Proof` (25 rows, 5 cols) | | |
| `Q3.1 Slot Assignment` (474 rows, 11 cols) | `Q3.1 Slotting Plan` (472 rows, 21 cols) | ⚠️ Different columns |
| `Q3.1 Heatmap (U-shape)` (placeholder) | | ❌ Missing |

**Reference `Q3.1 Slotting Design`** sections:
- **A** — Model assumptions (walk speed, round-trip factor, zone distances, pick rates)
- **B** — Zone design: Zone, Class, # SKU, Total picks, Pick mode, Slot rule, Dist to packing (m)

**Reference `Q3.1 Travel Proof`** sections:
- Distance-by-zone table (Zone A/B/C picks × dist × round-trip distance)
- OPTIMIZED vs. BASELINE results: total RT distance, avg dist/pick, travel time, reduction %, target

**Reference `Q3.1 Slot Assignment`** columns (11):
- Slot ID, SKU, Product, Cat, Zone, Order freq, Pick rate (pcs/min), Velocity (freq/rate), Size (L/pcs), Avg order qty, Dist (m)

**Current `Q3.1 Slotting Plan`** has 21 columns (much more detail, including sub_tier etc.) — this is fine, but we need a `Q3.1 Slot Assignment` sheet that matches the reference 11-column structure.

### Part 3 — Q3.2 Pick & Pack

| Reference | Current | Status |
|---|---|---|
| `Q3.2 Pick & Pack` (20 rows, 5 cols) | *(missing)* | ❌ Missing entirely |

**Reference `Q3.2 Pick & Pack`** contains:
- **A** — Two pick models: Channel, Order profile, Pick model, Mechanics, Pack
- **B** — Shared-SKU conflict allocation rules table: Rule, Condition, Action
- **C** — Decision flowchart (reference to chart image)

---

## Proposed Changes

### New Sheets to Add

| New Sheet Name | Based On | Content |
|---|---|---|
| `Q1.2 Heatmap Summary` | Reference `Q1.2 Heatmap` | Consolidated A/B/C sections from notes |
| `Q1.3 Order Profile Summary` | Reference `Q1.3 Order Profile` | Full comparison table from notes |
| `Q2.2 Lead Time` | Reference `Lead Time theo Vùng` | Mile-weighted LT computation table |
| `Q3.1 Slotting Design` | Reference `Q3.1 Slotting Design` | Model assumptions + zone design |
| `Q3.1 Travel Proof` | Reference `Q3.1 Travel Proof` | Distance table + baseline vs. optimized results |
| `Q3.1 Slot Assignment` | Reference `Q3.1 Slot Assignment` | Simplified 11-col slot assignment |
| `Q3.2 Pick & Pack` | Reference `Q3.2 Pick & Pack` | B2B/B2C pick models + conflict rules |

### Existing Sheets to Restructure

| Sheet | Changes Needed |
|---|---|
| `Q1.1 Dashboard` | **Rename to `Q1.1 ABC-XYZ`**; rearrange to match reference 6-section layout (A=qty, B=freq, C=XYZ vol, D=matrix vol, E=matrix freq, F=fast-moving). Currently has sections in different order with different column structure. |
| `Q2.2 Safety Stock` | Add formula header rows (SS formula, ROP formula) + restructure columns to match reference: SKU, Product, ABC-XYZ, μ_monthly, σ_monthly, μ_daily, σ_daily, Z, √LT, SS, ROP |
| `Q2.2 Inventory Pooling` | Expand from 5-row summary to full reference structure with sections A (channel types), B (channel definitions), C (allocation priority), D (operational mechanisms), E (quantification) |
| `Q3.1 Travel-Time Summary` | Split into 2 sheets: `Q3.1 Slotting Design` (model assumptions + zone design) and `Q3.1 Travel Proof` (travel-time computation) |

### Sheet Order in Workbook (Final)

```
Q1.1 ABC-XYZ             ← renamed dashboard, restructured to match reference sections
Q1.1 Full SKU Ranking    ← keep, fix column count if needed
Q1.1 Monthly Demand      ← keep
Q1.1 Class A Deep Dive   ← keep
Q1.2 Heatmap Summary     ← NEW: consolidated heatmap reference-format sheet
Q1.2 Warehouse by Region ← keep
Q1.2 Top Provinces       ← keep
Q1.2 Warehouse Imbalance ← keep
Q1.3 Order Profile Summary ← NEW: consolidated comparison sheet
Q1.3 Segment Profile     ← keep
Q1.3 Packaging           ← keep
Q1.3 Geo Spread          ← keep
Q2.1 HCM Districts       ← keep
Q2.1 Dark Store SLA      ← keep
Q2.2 Lead Time           ← NEW: mile-weighted LT table
Q2.2 Safety Stock        ← restructured columns + formula header
Q2.2 Inventory Pooling   ← expanded content
Q3.1 Slotting Design     ← NEW: model assumptions + zone design (from travel-time sheet)
Q3.1 Travel Proof        ← NEW: distance table + results (from travel-time sheet)
Q3.1 Slot Assignment     ← NEW: 11-col simplified assignment (from slotting_plan)
Q3.1 Slotting Plan       ← keep (full 21-col detail)
Q3.1 SKU Pick Profile    ← keep
Q3.2 Pick & Pack         ← NEW: B2B/B2C pick models + conflict rules
```

---

## Implementation Details

### Files to Modify

#### [MODIFY] [excel_reports.py](file:///Users/bunnypro/Projects/LOGage2026/src/logage2026/excel_reports.py)

1. **Rename `_write_dashboard()` → `_write_q11_abc_xyz_sheet()`** and restructure to 6 sections matching reference order (A=Qty ABC, B=Freq ABC, C=XYZ volatility, D=Qty×Volatility matrix, E=Qty×Freq matrix, F=Fast-Moving group).
2. **Add `_write_q12_heatmap_summary_sheet()`** — writes consolidated A/B/C sections.
3. **Add `_write_q13_order_profile_summary_sheet()`** — writes comparison table from `q13_segment_profile_summary` with all columns from reference.
4. **Add `_write_q22_lead_time_sheet()`** — writes methodology header + 6-region table + result row.
5. **Refactor `_write_q21_dark_store_sla_sheet()`** `Q2.2 Safety Stock` writer to match reference 11-column format with formula header rows.
6. **Expand `_write_q22_inventory_pooling()`** from 5-row to full reference structure with 5 sections (A–E).
7. **Add `_write_q31_slotting_design_sheet()`** — model assumptions (A) + zone design (B).
8. **Add `_write_q31_travel_proof_sheet()`** — zone distance table (A) + results (B).
9. **Add `_write_q31_slot_assignment_sheet()`** — 11-col simplified: Slot ID, SKU, Product, Cat, Zone, Order freq, Pick rate, Velocity, Size (L/pcs), Avg order qty, Dist(m).
10. **Add `_write_q32_pick_pack_sheet()`** — two-pick-models table + conflict rules table + flowchart reference.
11. **Update `write_summary_workbook()`** to create all sheets in the new order.

#### [MODIFY] [run_analysis.py](file:///Users/bunnypro/Projects/LOGage2026/run_analysis.py)

- Update the call to `write_summary_workbook()` to pass any new required DataFrames (e.g., the lead time table data currently in `notes.py`).
- The lead time region data is currently hardcoded in `notes.py` — needs to be surfaced as a DataFrame or dict from `analysis.py`.

#### [MODIFY] [analysis.py](file:///Users/bunnypro/Projects/LOGage2026/src/logage2026/analysis.py)

- Add **`build_lead_time_region_table()`** function that returns the 6-region DataFrame (Vùng, route type, LT days, orders, weight, quantity, LT×Orders, source) — currently hardcoded in `notes.py`.

---

## Resolved Decisions

- **Q1**: Rename `Q1.1 Dashboard` → `Q1.1 ABC-XYZ` and restructure in-place ✅
- **Q2**: Add new consolidated `Q1.2 Heatmap Summary` and `Q1.3 Order Profile Summary` alongside existing detailed sheets ✅
- **Q3**: Split `Q3.1 Travel-Time Summary` into `Q3.1 Slotting Design` + `Q3.1 Travel Proof` ✅

## Figure Insertion Plan

The reference Part 3 embeds chart images directly in two sheets. We will match this and extend to all analysis sheets:

| Sheet | Chart to Embed | File |
|---|---|---|
| `Q1.1 ABC-XYZ` | ABC Qty distribution + Freq matrix | `q11_abc_quantity_distribution.png`, `q11_abc_xyz_matrix_frequency.png`, `q11_abc_xyz_matrix_volatility.png` |
| `Q1.2 Heatmap Summary` | Warehouse imbalance + Region qty + Top provinces map | `q12_warehouse_imbalance.png`, `q12_region_quantity_orders.png`, `q12_top_demand_provinces_map.png` |
| `Q1.3 Order Profile Summary` | Order profile comparison + Packaging mix | `q13_order_profile_comparison.png`, `q13_packaging_mix.png` |
| `Q2.2 Lead Time` | Lead time sensitivity chart | `q22_lead_time_sensitivity.png` |
| `Q2.2 Inventory Pooling` | Inventory pooling impact chart | `q22_inventory_pooling.png` |
| `Q3.1 Slotting Design` | Slotting analysis overview | `q31_slotting_analysis.png` |
| `Q3.1 Heatmap (U-shape)` *(new sheet)* | U-shape warehouse heatmap | `q31_u_shape_heatmap.png` |
| `Q3.2 Pick & Pack` | Flowchart image (matches reference exactly) | `q32_pick_pack_flowchart.png` |

Images are inserted using `openpyxl.drawing.image.Image` — PIL 12.2.0 is confirmed available. Images placed below the data tables in each sheet, scaled to fit a standard display width (~700px).

---

## Verification Plan

### Automated
```bash
python run_analysis.py
# → Check that outputs/summary_table.xlsx contains all new sheets
# → Verify sheet count: should be 23 sheets (up from 18)
```

### Manual Verification
- Open `outputs/summary_table.xlsx` and compare each sheet against the corresponding reference file
- Check `Q1.1 ABC-XYZ` matches reference section order (A → B → C → D → E → F)
- Check `Q2.2 Lead Time` has 6 region rows + result row
- Check `Q3.1 Slot Assignment` has 470 data rows + 4 header rows = 474 total (matching reference)
- Check `Q3.2 Pick & Pack` has both pick-model table and conflict rules table
